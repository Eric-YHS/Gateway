GatewayDemo.Legacy-net472.fixed-20260723 交付验收摘要
生成日期：2026-07-23（Asia/Shanghai）

交付文件：GatewayDemo.Legacy-net472.fixed-20260723.zip
SHA-256：20ABF40FAEAC7E0B0ECC2C9DA3BA19A099135489B3CB6E5F786C4809F0486ACD
原始文件 SHA-256：222AFB7D8C588A4636C24F21E73771C3C51111308BDD7D33BE7395186E1C626D

结论
1. 已修复同名 Domain/host-only 设备 Cookie 导致刷新时重复创建设备的问题；候选凭据会有界枚举并选择数据库中可验证的设备。
2. 设备 Cookie 默认有效期为 365 天，每 30 天滑动续期；普通 gw_session 过期或被清除不会让已授权设备重新审核。
3. 清除设备 Cookie、设备 Cookie 长期过期、严格浏览器信号不匹配或管理员撤销授权时，才会要求重新建立或复核设备凭据；原申请、审批和审计记录不会被删除。
4. 数据库结构版本为 6，升级前执行在线一致性备份；迁移使用事务、跨进程初始化锁、完整性校验、关键数据计数校验和结构自愈。
5. 错误日志包含请求 ID、站点、状态码、耗时、异常链、上游、进程、程序集和 SQLite 版本；本次 502 实测未记录 Cookie、口令或授权头，主目录不可写时会降级到备用日志目录。

最终包检查
- 171 个条目，与原包的路径集合一致。
- Markdown 仅根目录 README.md 一个。
- 不含 bin、obj、PDB、数据库、日志、截图、HAR、trace、测试记录或验收记录。
- 不含 ChatGPT、OpenAI、Codex、Claude、Copilot 等助手文字，不含本机验收路径、机器名或验收账号凭据。
- 未发现环境专属硬编码。保留的固定值仅为 README 示例地址、本机演示后端 127.0.0.1:5055、默认演示用户名和 MSBuild 兼容探测路径；密码和口令默认均为空或部署时随机生成。

构建与依赖
- .NET Framework MSBuild 4.8.9221.0，Release 全量重建成功：0 警告、0 错误。
- 最终 ZIP 解压到全新目录后再次编译、部署成功。
- OSV 联网检查 16 个明确 NuGet 版本：未发现已知漏洞。

真实 Chromium 验收
- 新设备登记、申请、管理员登录、审批、受保护站点登录和访问均通过。
- 页面刷新前后设备编号、设备标识和 gw_device_credential 完全一致。
- gw_device_credential 为 host-only、Path=/、HttpOnly、SameSite=Lax；本地 HTTP 下 Secure=false，生产 HTTPS/ExternalHttps 策略下启用 Secure；到期时间为约 365 天。
- 删除 gw_session 后仍可访问，不触发重新审核；清除全部 Cookie 后生成新的设备编号并进入待审核。
- 两个相同浏览器环境产生不同设备，环境指纹一致但设备凭据互不复用。
- 撤销后旧凭据重放被挡，原设备可重新申请并再次审批通过。
- 报表、运维深链及刷新正常；未登录时静态 CSS/SVG 返回正确资源而不是网关 HTML。
- 390x844 移动视口下申请页和授权业务页文档宽度均为 390，无横向溢出。
- 正常流程浏览器控制台 0 错误、0 警告；受控上游断开时明确返回 502，并写入结构化错误日志，恢复配置后健康检查和业务页恢复。

数据库实测
- 最终 ZIP 新部署数据库：schema-version=6，PRAGMA quick_check=ok。
- 独立副本 v5→v6 升级：健康状态 200，升级前后 quick_check=ok。
- 关键记录计数升级前后保持一致：设备 6、申请 2、授权 2、审计 14。
- 升级前备份已创建并计算 SHA-256；测试后已恢复原数据库配置并清除副本。

扩展验证结果
- 代理差分 49/49；路由矩阵 38/38；配置变异 23/23。
- 动态安全检查 27/27；SQLite 并发操作 30/30、双进程冷启动 40/40、写锁恢复 20/20。
- 只读存储降级 6/6；上游超时、错误压缩、大头、SSE、断流、畸形 chunk、客户端取消、IIS 回收和恢复均通过。

原始证据目录：
E:\验证页面\acceptance-runs\gateway-publicbeta-cookie-20260722-200000\output\final
