# GatewayDemo.Legacy-net472 公测修复与验收报告

验收日期：2026-07-24  
验收对象：`GatewayDemo.Legacy-net472-fixed-20260724.zip`  
原始包：`GatewayDemo.Legacy-net472.zip`（保持不变）

## 结论

本次修复已通过独立 Release 重建、真实 IIS 部署、真实桌面与 Android 尺寸 Chromium 操作、原生移动接口请求、SQLite 并发与升级、故障恢复、安全边界和交付包卫生检查。

重点问题均已修复：

1. 原生移动 APP 申请不再采集或要求邮箱，仍强制校验企业口令。
2. 旧移动客户端即使继续发送 `X-Gateway-Applicant-Email`，网关也只会删除并忽略该头，不会保存、转发或重新启用邮箱激活。
3. 预期内的 401、403、429 凭据或限流响应不再被记录为应用异常。
4. Android/iOS 浏览器文档导航不会因移动 User-Agent 被误判为原生 APP；批准后可正常打开上游 HTML 登录页。
5. 数据库迁移增加完整性、行数和关键主键集合摘要校验；升级前备份经过完整 `PRAGMA integrity_check`。
6. 应用错误日志补充响应、根异常、进程、内存、线程池、版本和 SQLite 环境信息，同时避免记录凭据正文。

## 文件校验

| 文件 | SHA-256 |
| --- | --- |
| `GatewayDemo.Legacy-net472.zip` | `469D9AD95E0532B4E8300929480F9C3E9805F4E728C6CADDF576532490C7191A` |
| `GatewayDemo.Legacy-net472-fixed-20260724.zip` | `C8F377F58A58C6E5297B17818B805A24FE77887146A6E94F7461DDA36B21EF6F` |

## 重点验收结果

| 场景 | 结果 |
| --- | --- |
| 原包复现：正确企业口令、移动端无邮箱 | 已复现原包错误拒绝，确认修复对象准确 |
| 修复包：错误企业口令、无邮箱 | 返回兼容响应，原始状态 403；未创建设备或申请；应用错误日志增量 0 |
| 修复包：正确企业口令、无邮箱 | 成功创建待审核申请；数据库邮箱为空，`EmailActivationEligible=0` |
| 请求故意携带邮箱头 | 邮箱头被忽略；批准后再次登录数据库邮箱仍为空，激活资格仍为 0 |
| 企业口令要求 | 原生移动申请仍必须提供正确的 `X-Gateway-Access-Passcode` |
| 移动端管理 UI | 不显示邮箱、邮箱快速激活或激活回调 |
| 浏览器申请 UI | 正常保留邮箱和邮箱激活状态，未误伤浏览器流程 |
| 管理员真实浏览器批准 | 浏览器申请与移动申请均成功批准；管理页控制台错误 0 |
| 批准后的原生移动登录 | HTTP 200，`LoginStatus=1`；应用错误日志增量 0 |
| Android 尺寸真实 Chromium | 提交申请、批准后打开上游 HTML 登录、实际登录、进入门户均成功 |
| `/reports` 深链与刷新 | 导航和整页刷新均成功；浏览器控制台错误 0 |
| 撤销与凭据重放 | 撤销后旧 Cookie 立即失效；不同 User-Agent 重放被拒绝 |
| 重复浏览器申请 | 仅保留一个待审核申请，同时保留历史已批准记录 |
| 24 路并发相同移动申请 | 24/24 返回兼容响应；仅一个设备和一个待审核申请；无 SQLite lock/busy；错误日志增量 0 |
| 双 IIS Worker 数据库 v5 到 v6 升级 | 32/32 健康请求为 200；备份完整；架构与索引恢复；行数、ID、邮箱、状态和授权值全部保持 |
| 上游受控中断与恢复 | 中断时返回 502 并记录扩展根因诊断，无秘密泄漏；恢复后返回 200 |
| 配置文件损坏 | 自动使用 last-known-good；健康与路由保持 200；仅记录一次配置错误；原配置哈希精确恢复 |
| 安全与路由边界 | 管理端口业务路由、公共管理页、遍历、App_Data、TRACE/TRACK 均受阻；无 CORS 反射和 `Server` 头 |
| 代理差异 | 直接后端与网关业务响应一致；伪造转发元数据被替换 |
| SQLite 最终完整性 | `PRAGMA integrity_check` 返回 `ok` |

## 公测数据保护

修复未更改现有公测数据库文件，也未把验证数据库打入交付包。迁移验证使用隔离副本和预置历史数据完成，确认既有申请、邮箱、审核状态、授权关系和关键 ID 在 v5 到 v6 升级后保持不变。

移动端刷新已有待审核申请时会保留历史邮箱文本，以免破坏既有申请数据，但会继续保持 `EmailActivationEligible=0`，移动请求不能重新启用邮箱激活。部署时初始化器会先创建并校验升级备份；迁移后的结构、行数和关键主键摘要不一致时会失败退出，避免静默提交部分升级。

## 错误日志完善

错误记录新增或强化以下诊断信息：响应状态和内容类型、响应头是否已写入、外层与根异常类型/消息/HResult、内部异常深度、进程启动时间、工作集、应用池、线程、线程池余量、应用与核心版本、SQLite 版本和 CLR/位数信息。

预期内的认证失败和限流响应不会污染应用错误日志。受控 502 故障验证确认扩展信息足以定位上游，并且不会写入口令、密码或会话令牌。

## 交付包卫生

- ZIP 流式读取成功，共 214 个条目、202 个文件，独立解压后文件哈希全部一致。
- 独立 `Release Rebuild` 成功，0 error、0 warning。
- 仅有一个 Markdown 文件，且为根目录 `README.md`。
- 不含数据库、SQLite WAL/SHM、日志、备份、LKG、截图、测试结果或验收证据。
- 不含 AI、Codex、OpenAI、Playwright、测试报告或测试记录相关字样。
- 不含本次验收端口、账号密码、邮箱或分析目录路径。
- `PasscodeHash`、`PasswordHash`、通知授权和演示后端密码等敏感默认值均为空。
- 仅保留 README 明示的演示用户名、演示端口和回环地址；未发现硬编码口令、密码、哈希、令牌或生产地址。
- ZIP 路径安全检查未发现绝对路径或 `..` 路径穿越。

## 真实浏览器证据

证据保存在交付包外：

`E:\验证页面\analysis\gateway_publicbeta_20260724_102415\output\playwright`

关键截图：

- `r2-final-browser-submitted.png`
- `r2-final-admin-pending.png`
- `r2-final-admin-approved.png`
- `r2-final-mobile-browser-upstream-login.png`
- `r2-final-mobile-browser-portal.png`
- `r2-final-mobile-browser-reports-refresh.png`

所有 IIS 站点、验证账号、口令和数据库均为本机隔离验收资源，不属于交付内容或现有公测环境。
