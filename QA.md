# 统一认证网关 Demo — 演示技术 Q&A

> 本文档整理了远程演示时总裁可能提出的技术问题及参考答案。

> 当前仓库口径以 [需求边界说明.md](./需求边界说明.md) 为准：主需求是“给现有官方 APP 套网关”，`AppKey / AppSecret / HMAC` 这套自定义 APP 协议仅保留为可选增强项。

---

## 一、整体架构

### Q1：这个网关在整个系统里处于什么位置？

**答案：** 网关部署在 ERP 系统前面，作为唯一的公开入口。用户的所有请求先到网关，网关验证设备身份——通过后把请求反向代理转发给后端 ERP 系统，不通过则跳转到申请页。后端 ERP 放在内网，外部无法直接访问。ERP 系统本身零代码改动。

### Q2：技术栈是什么？为什么选这套？

**答案：** 当前 Demo 用的是 **.NET 10 + ASP.NET Core**。选这套的原因有三个：一是和你们现有的微软技术栈最贴近，团队理解和维护成本最低；二是反向代理直接使用微软官方的 **YARP**（Yet Another Reverse Proxy）库，适合做受控代理入口；三是数据层基于 **EF Core**，Demo 阶段先用 SQLite 跑通，正式部署再切到 SQL Server，迁移成本很低。

### Q3：网关是怎么把请求转发到后端的？

**答案：** 通过 YARP 反向代理实现。网关配置了多个站点路由规则，比如 `/proxy/erp-main/**` 转发到 ERP 后端，`/proxy/wms-east/**` 转发到 WMS 后端。转发时自动剥离 `/proxy/{siteKey}` 前缀，后端收到的请求路径和没有网关时完全一样，同时会给后端加一个 `X-Gateway-Site` 请求头标识来源站点，后端可选择使用或忽略。

---

## 二、安全性

### Q4：这个网关本身会不会成为新的攻击目标？攻破它就全完了？

**答案：** 网关确实是最关键的入口，但我们做了多层防护：
- **管理后台端口隔离**：5051 端口只绑定本机地址（127.0.0.1），外部完全无法访问
- **登录限流**：管理后台登录 5 分钟内最多 5 次尝试，防暴力破解
- **安全响应头**：X-Frame-Options DENY、Content-Security-Policy、X-Content-Type-Options、Referrer-Policy
- **审计日志**：所有访问和异常行为都有完整记录
- **设备指纹检测**：浏览器环境变化会触发重新审核

网关的代码量远小于 ERP 本体，攻击面其实比直接暴露 ERP 要小得多。

### Q5：设备号存在客户端 Cookie 里，会不会被窃取复制到别的机器上用？

**答案：** 浏览器端存的不是简单设备号，而是网关签发的**高强度随机令牌**。服务端数据库里存的是这个令牌的哈希，不存明文，所以数据库泄露也不能反推出浏览器里的原始令牌。  
如果攻击者把 Cookie 拿到另一台机器上直接复用，系统还会结合浏览器环境信号一起判断；只要 `UserAgent`、语言、平台这些关键特征发生变化，设备就会进入 `Challenged` 状态并要求重新审批。  
要强调的是，这不是“绝对防复制”，而是“复制 Cookie 还不够，必须同时伪装访问环境”。正式上线时建议叠加 HTTPS、审计告警，以及按客户需要追加 IP / 短信等第二因子。

### Q6：移动 APP 端怎么防伪造？手机上的东西更容易被抓包。

**答案：** 当前阶段对移动端的主要求，不是让现有官方 APP 全量改成一套新的签名协议，而是让**现有官方 APP 在尽量不改原运行机制的前提下也走统一认证网关**。  
也就是说，网关要兼容的是现有官方 APP 的：
- 企业站点地址解析
- 登录前初始化接口
- `userToken / SessionKey`
- `WCFService/PostBus.ashx/.svc`
- 相关 `ashx` 上传、附件、消息等链路

仓库里保留的 `AppKey / AppSecret / HMAC-SHA256` 这套能力，更多是给后续第三方 APP 或新开发 APP 做**可选增强项**，不是当前主线验收标准。

### Q7：我们原来 ERP 系统的安全措施还需要保留吗？

**答案：** 需要保留，而且必须保留。网关是**前置访问控制层**，解决的是"谁能进来"的问题。进来了之后，登录页的防暴力破解、强密码、双因素认证，以及系统内部的权限控制、SQL 注入防护等，仍然不可或缺。网关是"城墙"，ERP 本身的防御是"宫殿内部的安全"，两者互补，不是替代关系。

### Q8：数据加密是怎么做的？

**答案：** 当前实现主要做了三层保护：
1. **APP 密钥保护**：APP Secret 不以明文落库，而是用 ASP.NET Data Protection API 保护后再存储；Data Protection key ring 单独持久化，不和业务库混放
2. **密码哈希**：管理员密码使用 PBKDF2 加盐哈希保存，不存明文
3. **Cookie 安全**：管理后台 Session Cookie 使用 `HttpOnly + SameSite=Strict`；浏览器设备 Cookie 使用 `HttpOnly + SameSite=Lax`，在 HTTPS 场景下会自动带上 `Secure`

浏览器设备 Cookie 本身不是加密串，而是高熵随机令牌；真正敏感的 APP Secret 才是用 Data Protection 保护后入库的。

---

## 三、设备校验与动态 IP

### Q9：我们很多中小企业客户用的是动态 IP，这个怎么处理？

**答案：** 这正是我们选择**设备校验为主、IP 为辅**的原因。设备校验看的是设备本身（浏览器随机令牌 + 环境信号 / APP 签名），不依赖 IP 地址。客户换 IP 不会影响已授权设备的正常访问。同时系统会记录 IP 变化，如果同一个设备的 IP 跨地区跳变，审计日志里会有记录，管理员可以关注是否异常。

### Q10：客户用花生壳、内网穿透这类方案怎么办？

**答案：** 没有影响。花生壳和内网穿透本质上还是用户的浏览器在访问，设备指纹不变，已授权的设备一样能正常使用。网关验证的是"机器是谁"，不是"IP 是什么"。

### Q11：客户换电脑、升级浏览器、清 Cookie 了怎么办？

**答案：** 三种情况都有对应处理：
- **换电脑**：设备指纹不同，会被识别为新设备，需要重新申请审核
- **升级浏览器**：UserAgent 会变化，系统检测到后标记为"环境变化需重新审核"，用户重新提交申请，但管理员后台能看到这是"曾经授权过的设备"，可以快速审批
- **清 Cookie**：当前 Demo 版本里，浏览器设备令牌是保存在 Cookie 里的，Cookie 丢失后系统会把它视为新的浏览器设备，因此需要重新提交申请。后续如果希望把“清 Cookie 后快速恢复”做得更好，可以再叠加短信、已有账号确认，或者设备恢复机制

### Q12：能不能做到 IP + 设备双校验？

**答案：** 当前第一版以设备校验为主，但设计上预留了扩展能力。审计日志里已经在记录每个设备的 IP 地址，后续可以增加一个**策略模式**配置项，让管理员按客户情况选择：纯设备校验、纯 IP 白名单、IP + 设备双校验。不需要改架构，只需在校验逻辑里增加一个策略判断。

### Q13：设备指纹具体怎么生成的？

**答案：** 浏览器端提取三个 HTTP 请求头（UserAgent、AcceptLanguage、sec-ch-ua-platform），拼接后做 SHA-256 哈希，取前 12 位作为设备信号。这个信号不是设备 ID 本身，而是用来检测环境变化——如果信号变了，说明浏览器环境发生了变化，系统会要求重新审核。真正的浏览器设备标识是通过服务端颁发的随机令牌来实现的，不是靠客户端生成的。

---

## 四、多站点统一认证

### Q14：一个客户有多个站点，怎么实现统一认证？

**答案：** 网关支持配置多个受保护站点。每个站点有独立的 Key（如 `erp-main`、`wms-east`、`finance`），通过不同的 URL 路径访问。一个设备一次授权，管理员可以按站点粒度勾选允许访问哪些系统。比如客户的财务人员只能访问财务站点，仓库人员只能访问 WMS 站点，ERP 管理员可以全选。

### Q15：不同站点能不能有不同的管理员？

**答案：** 当前 Demo 版本是统一管理员。后续可以增加**多管理员 + 站点分权**功能。数据模型里已经有站点维度的关联表（`GatewayDeviceAuthorizationSite`），扩展不需要改底层结构。

### Q16：新增一个站点需要改代码吗？

**答案：** 不需要改代码。在配置文件里加一条站点配置即可，指定四个参数：站点 Key、显示名称、描述、后端地址。网关启动时自动加载所有站点配置并生成对应的代理路由。

---

## 五、移动端与多因子认证

### Q17：手机 APP 怎么接入？

**答案：** 当前主需求不是“开发一套新的 APP 协议让客户来适配”，而是让**现有官方 APP 也能被这层网关接住**。  
因此现阶段 APP 接入的重点是：
- 官方 APP 只改服务器地址或站点入口
- 网关兼容它原来的登录、`SessionKey / userToken`、`WCF / ashx` 请求链路
- 浏览器和官方 APP 一起纳入统一申请、审批、授权和审计体系

仓库里已有的 `AppKey / AppSecret / HMAC` 页面和能力继续保留，但当前应理解为**可选增强项**，不是官方 APP 的主用方式。

### Q18：短信验证码和微信认证什么时候能加上？

**答案：** 架构上已预留多因子认证的扩展点。短信验证码实现成本最低，接入短信服务商 SDK 即可，预计 2-3 个工作日。微信认证涉及微信开放平台对接，稍复杂一些，但也不涉及架构变更。建议先上设备校验 + 短信验证码，微信作为第二期。老板提到我们新版本已经做了手机短信和微信认证的接口，到时候直接对接。

---

## 六、部署与运维

### Q19：部署复杂吗？对客户现有环境改动大不大？

**答案：** 改动非常小。网关是独立容器部署，只需要：
1. 在服务器上部署 Docker（如果没有的话）
2. 把网关镜像和配置文件部署上去
3. 把 ERP 的外部访问端口改为仅内网可达，所有外部流量指向网关

ERP 系统本身**零代码改动**。

### Q20：网关挂了是不是所有客户都访问不了了？

**答案：** 这是单点风险，但解决方案很成熟：
- **容器自动重启**：Docker 支持进程崩溃后自动重启
- **健康检查**：网关提供了 `/healthz` 端点，可配合负载均衡器做健康检测
- **集群部署**：正式版本支持多实例负载均衡，数据库和密钥通过共享存储同步
- **紧急旁路**：可设置紧急开关，网关故障时临时关闭验证直接放行

第一版 Demo 验证核心逻辑，高可用是产品化阶段的工作。

### Q21：客户的服务器都是 Windows，支持吗？

**答案：** 完全支持。网关用 .NET 开发，天然支持 Windows。当前 Demo 是 **.NET 10**，Windows、Linux 都能跑。考虑到客户环境，也可以用 **IIS 反向代理 + ASP.NET Core 进程** 方式部署，不强制 Docker。但推荐容器化，隔离性更好，运维更标准化。

### Q22：数据存储用的什么数据库？客户需要额外采购吗？

**答案：** Demo 版本用 SQLite（轻量级文件数据库），不需要额外安装。正式部署一行配置切换到 SQL Server，客户现有的 SQL Server 直接用，不需要额外采购。

### Q23：网关和 ERP 之间是网络隔离的吗？

**答案：** 是的。网关在**边缘网络**（对外暴露），ERP 后端在**内部网络**（Docker 中标记为 `internal: true`）。ERP 只对网关可达，外部流量完全无法直接访问 ERP。物理部署时通过防火墙规则达到同样效果。

### Q24：ERP 后端需要知道网关的存在吗？

**答案：** 不需要。网关转发请求时会自动添加 `X-Gateway-Site` 请求头标识来源站点，ERP 可以忽略。网关对后端完全透明，ERP 不需要做任何适配。

---

## 七、对接现有系统

### Q25：我们原来 ERP 的申请审核功能能不能复用？

**答案：** 可以复用，以**接口对接**的方式而不是代码层面耦合。网关通过 API 把申请数据传给 ERP 后台，ERP 的审核流程不变，审核结果通过回调通知网关。两个系统各自独立，只通过 API 交互。老板也确认了申请审核接口等方案确认后再做。

---

## 八、审计与运维

### Q26：审计日志记录了什么？

**答案：** 所有关键操作都有记录：设备首次注册、访问申请提交、管理员审批通过/拒绝、设备授权/撤销、每次访问放行/拦截（含拦截原因和目标站点）。每条记录包含：事件类型、设备ID、设备编码、站点Key、客户端IP、时间戳。后续可对接日志分析系统做异常告警。

### Q27：系统用了哪些开源组件？有没有许可证风险？

**答案：** 全部是微软官方或主流开源组件：
- **YARP**：微软官方反向代理库，MIT 许可
- **ASP.NET Core**：微软官方框架，MIT 许可
- **Entity Framework Core**：微软官方 ORM，MIT 许可
- **Docker**：Apache 2.0 许可

全部是 MIT 或 Apache 2.0 许可证，没有 GPL 等传染性许可证风险，商用没有问题。

---

## 九、后续技术规划

### Q28：从 Demo 到能上线，技术上还需要做什么？

| 工作项 | 说明 |
|--------|------|
| 短信/微信多因子认证 | 对接现有短信和微信认证接口 |
| 申请审核接口对接 | 对接 ERP 原有审核流程 |
| 多管理员分权 | 按站点分配管理权限 |
| 高可用集群 | 多实例 + 负载均衡 + 共享存储 |
| 配置管理界面 | 站点配置可视化，不用改配置文件 |
| 客户批量导入 | Excel/CSV 批量导入设备白名单 |
| 通知机制 | 邮件/短信通知管理员审核，通知客户结果 |
| 安全加固与渗透测试 | 正式上线前的安全审计 |

---

## 十、补充技术追问

### Q29：为什么不直接把这套访问控制做到 ERP 里面，而要单独做一个网关？

**答案：** 因为这套能力本质上是“前置访问控制”，它更适合作为独立层存在，而不是耦合进 ERP 本体。单独做网关有四个直接好处：
- ERP 本体尽量零改动，实施风险更低
- 一个网关可以同时保护 ERP、WMS、财务等多个站点
- 访问控制逻辑和业务逻辑解耦，后续升级不会牵动 ERP
- 安全边界更清晰，公网只暴露网关，后端系统只留内网访问

### Q30：如果客户前面还有 IIS、SLB、WAF 或 Nginx，真实客户端 IP 怎么拿？会不会被伪造 `X-Forwarded-For`？

**答案：** 当前实现已经支持标准的 `ForwardedHeaders` 处理。网关不会无条件相信所有转发头，而是只信任配置里的**可信代理地址或 CIDR 段**。  
也就是说，只有来自受信任反向代理的 `X-Forwarded-For`、`X-Forwarded-Proto`、`X-Forwarded-Host` 才会被采纳；如果请求不是从可信代理过来，就按直连方式处理。这样可以避免客户端自己伪造转发头影响审计和策略判断。

### Q31：为什么管理后台一定要独立端口，而不是放在同一个域名下隐藏一个路径？

**答案：** 因为“隐藏路径”只是降低可见度，不是真正减少攻击面。独立管理端口的好处是：
- 公网业务端口根本不挂管理页面，请求 `/admin/login` 直接 404
- 可以配合防火墙、宿主机绑定或堡垒机，把管理口只开放给运维侧
- 业务流量和管理流量天然分离，审计和访问控制都更清楚

当前 Demo 里，管理面不仅是不同路径，而且还会检查**实际接收端口**，只有到管理端口的请求才会进入后台逻辑。

### Q32：容器重建、服务重启后，已经审批通过的设备和 APP 密钥会不会全部失效？

**答案：** 只要两样东西保住，就不会全部失效：
1. 业务数据库
2. Data Protection key ring

审批结果、设备记录、审计日志都在数据库里；如果启用了可选的自定义 APP 密钥模式，APP Secret 虽然不是明文存储，但只要 key ring 还在，就还能解密并继续使用。  
这也是为什么 Demo 里专门把数据卷和 key ring 卷分开持久化。  
如果只是重启进程或重建容器，但数据库和 key ring 都保留，已授权浏览器设备，以及可选的自定义 APP 密钥都能继续工作。

### Q33：如果重新签发 APP 密钥，或者撤销设备放行，旧密钥会怎么样？

**答案：** 这个问题针对的是**可选的自定义 APP 密钥模式**。在这套模式下，旧密钥会立即失效，不会和新密钥并存。当前实现里：
- 重新签发 APP 密钥时，会覆盖旧的 `AppKey/AppSecret`
- 撤销设备放行时，会同步清空设备上的 APP 密钥
- 与旧密钥相关的防重放 nonce 记录也会一起清理

并且即使后面重新审批通过，**旧 APP 密钥也不会自动恢复**，必须重新签发。这是刻意设计的，避免被撤销的旧密钥在之后又被“意外复活”。

### Q34：如果同一台设备连续提交多次申请，或者管理员重复审批，会不会把数据搞乱？

**答案：** 不会。当前实现做了两个收口：
- 对于同一设备，如果已经有一条 `Pending` 申请，新提交会更新原申请，而不是不断新增待审批单
- 如果这台设备已经有已批准授权，再次审批时会把新批准的站点权限合并进去，而不是生成多条并行生效的授权

也就是说，数据模型里同时保留“申请记录”和“当前有效授权”，前者负责过程留痕，后者负责实时放行判断。

### Q35：如果有人故意访问一个不存在的站点 Key，或者乱拼代理路径，会发生什么？

**答案：** 会被直接拦掉，不会打到后端。当前逻辑里：
- 不存在的站点 Key 会直接返回 404
- 如果只访问到 `/proxy/{siteKey}` 而没有尾部路径，会先规范化跳到带斜杠的路径
- 即使站点存在，只要设备没授权对应站点，也会被重定向回 `/gateway`

换句话说，站点 Key 既是路由标识，也是授权判断的一部分，不是随便拼个 URL 就能越权访问。

### Q36：这个网关未来做高可用时，是有状态还是无状态？能不能横向扩？

**答案：** 网关进程本身是轻状态的，真正需要共享的是两部分：
- 数据库里的设备、申请、授权、审计数据
- Data Protection key ring

所以正式做多实例时，思路很清楚：前面挂负载均衡，后面多个网关实例共享同一套数据库和 key ring 存储即可。  
Demo 目前是单实例，目的是先把核心闭环跑通；产品化阶段再把部署形态扩成集群。

### Q37：YARP 会不会成为性能瓶颈？所有流量都过它，压力会不会太大？

**答案：** 正常情况下不会成为第一瓶颈。原因是这层网关做的事情相对集中：
- 读 Cookie 或签名头
- 查当前设备授权
- 判断站点是否允许
- 然后把请求转发给上游

真正重的业务处理仍然在 ERP、WMS 这些后端系统里。YARP 本身就是专门做高性能反向代理的，适合做这种前置控制层。  
如果后续流量继续放大，也可以继续优化，比如把热点授权结果做缓存、把数据库切到 SQL Server、网关做多实例横向扩展。

### Q38：如果客户要求把网关放在 DMZ，后端继续留在内网，这套结构能不能适配？

**答案：** 完全适配，而且这正是推荐形态。公网或 DMZ 只暴露网关，ERP 后端继续放在内网，后端只接受来自网关或内网代理的访问。  
Demo 的 Docker 编排里已经模拟了这个边界：网关在外层网络，后端在 `internal` 网络，不对宿主机暴露端口。正式部署时换成真实防火墙和网络区域即可。
